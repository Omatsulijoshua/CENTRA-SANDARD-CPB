PORT=3000
USE_FIREBASE=1

# Use ONE of these credential methods:
# 1) Point to a Firebase service account JSON file (recommended)
FIREBASE_SERVICE_ACCOUNT_FILE=""
# 2) Or paste the JSON as a single line (advanced)
FIREBASE_SERVICE_ACCOUNT_JSON=""

# Optional: set to your Firebase project id to enforce token audience
FIREBASE_PROJECT_ID=""
